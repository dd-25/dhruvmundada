---
title: My first review comment was about a folder
source: ConfluxSys
kind: work
date: 2024-06
order: 10
audiences: ["*"]
points:
  - A Java monorepo where every request went controller, then service, then repository — and putting logic in the wrong one was a review comment, not a preference.
  - Spring Boot and JDBC against a live database, built through Maven.
  - "The rest was what a team assumes you already know: branches, stashes, review before merge, and configuration read from the environment rather than typed into a file that ships."
---

Five weeks, after second year. Until then a project was one folder and one file
that did everything, and layers were a diagram in a lecture.

The reason they exist landed the first time a change stayed inside one of them.
