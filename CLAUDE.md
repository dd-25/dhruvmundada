@AGENTS.md

## Read before editing

- `AGENTS.md` — architecture, the content-is-data rule, the checks to run.
- `docs/VOICE.md` — **required before changing anything under `content/`.** Who each
  lens is for, the done-versus-learnt split, the register per section, and the list
  of things that must never be published.
- `docs/CONTENT.md` — mechanics: which file, which field, which YAML traps.
- `docs/DECISIONS.md` — what was settled and what was rejected, with reasons. Check
  it before re-proposing something.
- `scratchpad/` — Dhruv's raw words and the fact-by-fact mapping. Gitignored, so it
  may be absent; if it is, ask rather than inferring intent from edited output.
