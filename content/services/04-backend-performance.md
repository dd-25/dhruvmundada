---
name: "Is it slow exactly when you are busiest?"
blurb: "It works until the traffic arrives. Then it is slow, expensive, or down at the worst moment."
order: 4
audiences: ["business"]
points:
  - Root-cause analysis from production signals, not staging guesses.
  - Connection pooling, timeouts and leak hunting across HTTP, database and cache clients.
  - Query, index and pagination work aimed at where the time is actually going.
  - Load testing, so the next limit is known before your customers find it.
---
